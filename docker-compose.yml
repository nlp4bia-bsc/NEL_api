services:
  ner-linking-api:
    container_name: ner-linking-api
    build: .
    user: root
    environment:
      CUDA_VISIBLE_DEVICES: ""
      LANGUAGE: es
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    restart: unless-stopped
    privileged: true

networks:
  default:
    name: cogstack-net
    external: true
